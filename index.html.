<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MJ Traiteur — Traiteur Événementiel</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
  :root {
    --black: #0a0a0a;
    --deep: #111111;
    --gold: #b8964e;
    --gold-light: #d4af72;
    --gold-pale: #e8d5a3;
    --white: #f5f0e8;
    --grey: #888880;
  }

  *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

  html { scroll-behavior: smooth; }

  body {
    background: var(--black);
    color: var(--white);
    font-family: 'Montserrat', sans-serif;
    font-weight: 300;
    overflow-x: hidden;
  }

  /* NOISE TEXTURE OVERLAY */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 1000;
    opacity: 0.4;
  }

  /* NAV */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    padding: 24px 60px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: linear-gradient(to bottom, rgba(10,10,10,0.95), transparent);
    transition: all 0.4s ease;
  }

  nav.scrolled {
    background: rgba(10,10,10,0.97);
    padding: 16px 60px;
    border-bottom: 1px solid rgba(184,150,78,0.15);
  }

  .nav-logo {
    font-family: 'Cormorant Garamond', serif;
    font-size: 22px;
    font-weight: 600;
    letter-spacing: 0.15em;
    color: var(--white);
    text-decoration: none;
  }

  .nav-logo span { color: var(--gold); }

  .nav-links {
    display: flex;
    gap: 40px;
    list-style: none;
  }

  .nav-links a {
    font-size: 11px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--grey);
    text-decoration: none;
    transition: color 0.3s;
  }

  .nav-links a:hover { color: var(--gold); }

  .nav-cta {
    font-size: 11px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--gold) !important;
    border: 1px solid rgba(184,150,78,0.4) !important;
    padding: 10px 24px !important;
    transition: all 0.3s !important;
  }

  .nav-cta:hover {
    background: var(--gold) !important;
    color: var(--black) !important;
  }

  /* HERO */
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
    text-align: center;
    padding: 120px 40px 80px;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    background: 
      radial-gradient(ellipse 80% 60% at 50% 40%, rgba(184,150,78,0.06) 0%, transparent 70%),
      radial-gradient(ellipse 40% 40% at 20% 80%, rgba(184,150,78,0.03) 0%, transparent 60%);
  }

  .hero-line {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 600px;
    height: 600px;
    border: 1px solid rgba(184,150,78,0.06);
    border-radius: 50%;
    animation: rotate 30s linear infinite;
  }

  .hero-line:nth-child(2) {
    width: 400px;
    height: 400px;
    border-color: rgba(184,150,78,0.04);
    animation-direction: reverse;
    animation-duration: 20s;
  }

  @keyframes rotate { to { transform: translate(-50%, -50%) rotate(360deg); } }

  .hero-content {
    position: relative;
    z-index: 2;
    max-width: 800px;
  }

  .hero-eyebrow {
    font-size: 10px;
    letter-spacing: 0.4em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 32px;
    opacity: 0;
    animation: fadeUp 1s ease 0.3s forwards;
  }

  .hero-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(72px, 12vw, 130px);
    font-weight: 300;
    line-height: 0.9;
    letter-spacing: -0.02em;
    margin-bottom: 8px;
    opacity: 0;
    animation: fadeUp 1s ease 0.5s forwards;
  }

  .hero-title .gold { color: var(--gold); }

  .hero-subtitle {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(18px, 3vw, 28px);
    font-weight: 300;
    font-style: italic;
    color: var(--grey);
    letter-spacing: 0.1em;
    margin-bottom: 48px;
    opacity: 0;
    animation: fadeUp 1s ease 0.7s forwards;
  }

  /* GOOGLE BADGE */
  .google-badge {
    display: inline-flex;
    align-items: center;
    gap: 16px;
    border: 1px solid rgba(184,150,78,0.3);
    padding: 14px 28px;
    margin-bottom: 56px;
    background: rgba(184,150,78,0.04);
    opacity: 0;
    animation: fadeUp 1s ease 0.9s forwards;
    cursor: pointer;
    text-decoration: none;
    transition: all 0.3s;
  }

  .google-badge:hover {
    background: rgba(184,150,78,0.1);
    border-color: var(--gold);
  }

  .stars { color: var(--gold); font-size: 18px; letter-spacing: 2px; }

  .badge-text {
    text-align: left;
    font-size: 11px;
    letter-spacing: 0.1em;
    color: var(--white);
  }

  .badge-text strong {
    display: block;
    font-size: 13px;
    font-weight: 500;
    color: var(--gold-light);
  }

  .hero-divider {
    width: 1px;
    height: 80px;
    background: linear-gradient(to bottom, var(--gold), transparent);
    margin: 0 auto;
    opacity: 0;
    animation: fadeUp 1s ease 1.1s forwards;
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* SECTION BASE */
  section { padding: 120px 60px; }

  .section-label {
    font-size: 10px;
    letter-spacing: 0.4em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 16px;
    display: block;
  }

  .section-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(42px, 6vw, 72px);
    font-weight: 300;
    line-height: 1.05;
    margin-bottom: 24px;
  }

  .section-body {
    font-size: 14px;
    line-height: 2;
    color: var(--grey);
    max-width: 500px;
  }

  /* SERVICES */
  #services {
    background: var(--deep);
    position: relative;
    overflow: hidden;
  }

  #services::before {
    content: '';
    position: absolute;
    top: -200px; right: -200px;
    width: 600px; height: 600px;
    background: radial-gradient(circle, rgba(184,150,78,0.04) 0%, transparent 70%);
    pointer-events: none;
  }

  .services-header {
    text-align: center;
    margin-bottom: 80px;
  }

  .services-header .section-body { margin: 0 auto; text-align: center; }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2px;
    max-width: 1100px;
    margin: 0 auto;
  }

  .service-card {
    background: var(--black);
    padding: 52px 40px;
    position: relative;
    overflow: hidden;
    cursor: default;
    transition: all 0.5s ease;
    group: true;
  }

  .service-card::before {
    content: '';
    position: absolute;
    bottom: 0; left: 0;
    width: 100%; height: 2px;
    background: var(--gold);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.5s ease;
  }

  .service-card:hover { background: #141414; }
  .service-card:hover::before { transform: scaleX(1); }

  .service-number {
    font-family: 'Cormorant Garamond', serif;
    font-size: 64px;
    font-weight: 300;
    color: rgba(184,150,78,0.08);
    position: absolute;
    top: 20px; right: 30px;
    line-height: 1;
    transition: color 0.5s;
  }

  .service-card:hover .service-number { color: rgba(184,150,78,0.15); }

  .service-icon {
    font-size: 28px;
    margin-bottom: 24px;
    display: block;
  }

  .service-name {
    font-family: 'Cormorant Garamond', serif;
    font-size: 26px;
    font-weight: 400;
    margin-bottom: 12px;
    color: var(--white);
  }

  .service-desc {
    font-size: 12px;
    line-height: 1.8;
    color: var(--grey);
  }

  /* ABOUT / ZONES */
  #about {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 120px;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
  }

  .about-visual {
    position: relative;
  }

  .about-logo-frame {
    width: 100%;
    aspect-ratio: 1;
    background: var(--deep);
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
  }

  .about-logo-frame::after {
    content: '';
    position: absolute;
    inset: 20px;
    border: 1px solid rgba(184,150,78,0.15);
  }

  .mj-monogram {
    font-family: 'Cormorant Garamond', serif;
    font-size: 160px;
    font-weight: 600;
    color: var(--white);
    line-height: 1;
    position: relative;
    z-index: 1;
  }

  .mj-monogram .cutlery {
    position: absolute;
    font-size: 48px;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
    color: var(--gold);
  }

  .zone-tag {
    display: inline-block;
    font-size: 10px;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--gold);
    border: 1px solid rgba(184,150,78,0.3);
    padding: 8px 16px;
    margin: 4px 4px 4px 0;
  }

  /* STATS BAR */
  .stats-bar {
    background: var(--gold);
    padding: 0;
    display: flex;
    justify-content: center;
  }

  .stats-inner {
    display: flex;
    width: 100%;
    max-width: 1200px;
  }

  .stat {
    flex: 1;
    padding: 48px 40px;
    text-align: center;
    border-right: 1px solid rgba(0,0,0,0.1);
  }

  .stat:last-child { border-right: none; }

  .stat-num {
    font-family: 'Cormorant Garamond', serif;
    font-size: 56px;
    font-weight: 300;
    color: var(--black);
    line-height: 1;
    display: block;
    margin-bottom: 8px;
  }

  .stat-label {
    font-size: 10px;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: rgba(0,0,0,0.6);
  }

  /* CONTACT */
  #contact {
    background: var(--deep);
    text-align: center;
    padding: 140px 60px;
  }

  .contact-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(48px, 8vw, 100px);
    font-weight: 300;
    line-height: 1;
    margin-bottom: 16px;
  }

  .contact-sub {
    font-family: 'Cormorant Garamond', serif;
    font-style: italic;
    font-size: 20px;
    color: var(--grey);
    margin-bottom: 72px;
  }

  .contact-grid {
    display: flex;
    justify-content: center;
    gap: 2px;
    flex-wrap: wrap;
    max-width: 900px;
    margin: 0 auto 64px;
  }

  .contact-card {
    background: var(--black);
    padding: 40px 48px;
    flex: 1;
    min-width: 220px;
    text-decoration: none;
    transition: all 0.3s;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 16px;
  }

  .contact-card:hover {
    background: #141414;
    transform: translateY(-4px);
  }

  .contact-icon { font-size: 28px; }

  .contact-label {
    font-size: 10px;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: var(--gold);
  }

  .contact-value {
    font-size: 13px;
    color: var(--white);
    letter-spacing: 0.05em;
  }

  .cta-main {
    display: inline-block;
    font-size: 11px;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: var(--black);
    background: var(--gold);
    padding: 20px 56px;
    text-decoration: none;
    transition: all 0.3s;
    position: relative;
    overflow: hidden;
  }

  .cta-main::after {
    content: '';
    position: absolute;
    inset: 0;
    background: var(--gold-light);
    transform: translateX(-100%);
    transition: transform 0.4s ease;
  }

  .cta-main:hover::after { transform: translateX(0); }
  .cta-main span { position: relative; z-index: 1; }

  /* FOOTER */
  footer {
    padding: 48px 60px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-top: 1px solid rgba(184,150,78,0.1);
    flex-wrap: wrap;
    gap: 24px;
  }

  .footer-logo {
    font-family: 'Cormorant Garamond', serif;
    font-size: 18px;
    font-weight: 600;
    letter-spacing: 0.15em;
    color: var(--white);
  }

  .footer-logo span { color: var(--gold); }

  .footer-copy {
    font-size: 11px;
    color: var(--grey);
    letter-spacing: 0.1em;
  }

  .footer-social a {
    font-size: 11px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--gold);
    text-decoration: none;
    border-bottom: 1px solid rgba(184,150,78,0.3);
    padding-bottom: 2px;
    transition: border-color 0.3s;
  }

  .footer-social a:hover { border-color: var(--gold); }

  /* MOBILE */
  @media (max-width: 900px) {
    nav { padding: 20px 24px; }
    nav.scrolled { padding: 14px 24px; }
    .nav-links { display: none; }
    section { padding: 80px 24px; }
    .services-grid { grid-template-columns: 1fr; }
    #about { grid-template-columns: 1fr; gap: 48px; padding: 80px 24px; }
    .stats-inner { flex-direction: column; }
    .stat { border-right: none; border-bottom: 1px solid rgba(0,0,0,0.1); }
    .stat:last-child { border-bottom: none; }
    #foodtruck > div > div:first-child,
    #foodtruck > div { grid-template-columns: 1fr !important; gap: 48px !important; }
    footer { padding: 36px 24px; flex-direction: column; text-align: center; }
    .mj-monogram { font-size: 100px; }
  }

  /* SCROLL REVEAL */
  .reveal {
    opacity: 0;
    transform: translateY(40px);
    transition: opacity 0.8s ease, transform 0.8s ease;
  }

  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .reveal-delay-1 { transition-delay: 0.1s; }
  .reveal-delay-2 { transition-delay: 0.2s; }
  .reveal-delay-3 { transition-delay: 0.3s; }
  .reveal-delay-4 { transition-delay: 0.4s; }
  .reveal-delay-5 { transition-delay: 0.5s; }
  .reveal-delay-6 { transition-delay: 0.6s; }
</style>
</head>
<body>

<!-- NAV -->
<nav id="nav">
  <a href="#" class="nav-logo"><span>MJ</span> Traiteur</a>
  <ul class="nav-links">
    <li><a href="#services">Services</a></li>
    <li><a href="#about">Notre histoire</a></li>
    <li><a href="#foodtruck">Food Truck</a></li>
    <li><a href="#contact">Contact</a></li>
    <li><a href="tel:0666218892" class="nav-cta">Demande de devis</a></li>
  </ul>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="hero-bg"></div>
  <div class="hero-line"></div>
  <div class="hero-line"></div>

  <div class="hero-content">
    <p class="hero-eyebrow">Traiteur Événementiel — PACA</p>
    <h1 class="hero-title">
      <span class="gold">MJ</span><br>Traiteur
    </h1>
    <p class="hero-subtitle">L'art de la table pour vos moments d'exception</p>

    <a href="https://share.google/fCRqLrAyAk4pEcodw" target="_blank" class="google-badge">
      <div class="stars">★★★★★</div>
      <div class="badge-text">
        <strong>5,0 / 5 sur Google</strong>
        +80 avis vérifiés · MJ Traiteur Événementiel
      </div>
    </a>

    <div class="hero-divider"></div>
  </div>
</section>

<!-- STATS BAR -->
<div class="stats-bar">
  <div class="stats-inner">
    <div class="stat">
      <span class="stat-num">+80</span>
      <span class="stat-label">Avis Google</span>
    </div>
    <div class="stat">
      <span class="stat-num">5★</span>
      <span class="stat-label">Note parfaite</span>
    </div>
    <div class="stat">
      <span class="stat-num">100%</span>
      <span class="stat-label">Sur mesure</span>
    </div>
    <div class="stat">
      <span class="stat-num">PACA</span>
      <span class="stat-label">& sur demande</span>
    </div>
  </div>
</div>

<!-- SERVICES -->
<section id="services">
  <div class="services-header reveal">
    <span class="section-label">Ce que nous proposons</span>
    <h2 class="section-title">Chaque événement<br><em style="font-style:italic; color: var(--gold-light);">mérite l'excellence</em></h2>
    <p class="section-body">Du repas intime au grand banquet, MJ Traiteur compose pour vous des prestations soignées et généreuses, adaptées à chaque occasion.</p>
  </div>

  <div class="services-grid">
    <div class="service-card reveal reveal-delay-1">
      <span class="service-number">01</span>
      <span class="service-icon">💍</span>
      <h3 class="service-name">Mariage</h3>
      <p class="service-desc">Le plus beau jour de votre vie mérite un traiteur à la hauteur. Cocktail, dîner assis, buffet — nous créons l'expérience dont vous rêvez.</p>
    </div>
    <div class="service-card reveal reveal-delay-2">
      <span class="service-number">02</span>
      <span class="service-icon">🥂</span>
      <h3 class="service-name">Anniversaire</h3>
      <p class="service-desc">Célébrez en grand avec une prestation culinaire qui marque les esprits et ravit tous les convives.</p>
    </div>
    <div class="service-card reveal reveal-delay-3">
      <span class="service-number">03</span>
      <span class="service-icon">🏛️</span>
      <h3 class="service-name">Séminaire</h3>
      <p class="service-desc">Pauses déjeuner, cocktails d'accueil, dîners de gala — sublimez vos événements professionnels.</p>
    </div>
    <div class="service-card reveal reveal-delay-4">
      <span class="service-number">04</span>
      <span class="service-icon">👶</span>
      <h3 class="service-name">Baptême</h3>
      <p class="service-desc">Un moment tendre et chaleureux, rehaussé par une cuisine généreuse et raffinée pour toute la famille.</p>
    </div>
    <div class="service-card reveal reveal-delay-5">
      <span class="service-number">05</span>
      <span class="service-icon">✨</span>
      <h3 class="service-name">Événement privé</h3>
      <p class="service-desc">Réunion de famille, soirée entre amis, fête de fin d'année — chaque événement mérite une attention particulière.</p>
    </div>
    <div class="service-card reveal reveal-delay-6">
      <span class="service-number">06</span>
      <span class="service-icon">🎯</span>
      <h3 class="service-name">Sur mesure</h3>
      <p class="service-desc">Votre projet ne rentre dans aucune case ? Contactez-nous. Nous construisons ensemble la prestation parfaite.</p>
    </div>
  </div>
</section>

<!-- ABOUT -->
<section style="padding: 140px 60px; overflow: hidden;">
  <div id="about">
    <div class="about-visual reveal">
      <div class="about-logo-frame">
        <div style="text-align:center; position: relative; z-index: 1;">
          <div class="mj-monogram">MJ</div>
          <div style="color: var(--gold); font-size: 36px; margin-top: -10px;">🍴</div>
        </div>
        <div style="position:absolute; inset:0; background: radial-gradient(circle at 50% 50%, rgba(184,150,78,0.06), transparent 70%);"></div>
      </div>
    </div>

    <div class="reveal reveal-delay-2">
      <span class="section-label">Notre engagement</span>
      <h2 class="section-title">Une passion,<br><em style="font-style:italic; color:var(--gold-light);">votre plaisir</em></h2>
      <p class="section-body" style="margin-bottom: 40px;">MJ Traiteur, c'est l'alliance d'une cuisine généreuse, d'un service attentionné et d'une vraie capacité d'adaptation à vos envies. Basés en région PACA et dans les Bouches-du-Rhône, nous nous déplaçons également sur tout autre secteur sur demande.</p>

      <p style="font-size: 12px; letter-spacing: 0.2em; text-transform: uppercase; color: var(--gold); margin-bottom: 16px;">Zones d'intervention</p>
      <div>
        <span class="zone-tag">PACA</span>
        <span class="zone-tag">Bouches-du-Rhône</span>
        <span class="zone-tag">Var</span>
        <span class="zone-tag">Vaucluse</span>
        <span class="zone-tag">Autres sur demande</span>
      </div>
    </div>
  </div>
</section>

<!-- FOOD TRUCK -->
<section id="foodtruck" style="padding: 140px 60px; background: var(--black); position: relative; overflow: hidden;">

  <div style="position:absolute; top:0; left:0; right:0; height:2px; background: linear-gradient(to right, transparent, var(--gold), transparent);"></div>

  <div style="max-width: 1100px; margin: 0 auto; display: grid; grid-template-columns: 1fr 1fr; gap: 100px; align-items: center;">

    <!-- Visual -->
    <div class="reveal" style="position: relative;">
      <div style="background: var(--deep); padding: 64px 48px; position: relative; overflow: hidden;">
        <div style="position:absolute; inset:0; background: radial-gradient(ellipse at 30% 60%, rgba(184,150,78,0.07), transparent 70%);"></div>
        <div style="position:absolute; top:20px; right:20px; bottom:20px; left:20px; border:1px solid rgba(184,150,78,0.1);"></div>
        <div style="position: relative; z-index: 1; text-align: center;">
          <div style="font-size: 80px; margin-bottom: 24px;">🚚</div>
          <p style="font-family: 'Cormorant Garamond', serif; font-size: 36px; font-weight: 300; color: var(--white); line-height: 1.1; margin-bottom: 20px;">Le Food Truck<br><em style="color: var(--gold);">MJ Traiteur</em></p>
          <p style="font-size: 10px; letter-spacing: 0.3em; text-transform: uppercase; color: var(--grey);">Disponible sur demande</p>
        </div>
        <!-- Menu tags -->
        <div style="margin-top: 40px; display: flex; flex-wrap: wrap; gap: 8px; justify-content: center; position: relative; z-index:1;">
          <span style="font-size: 11px; letter-spacing: 0.15em; text-transform: uppercase; color: var(--gold); border: 1px solid rgba(184,150,78,0.35); padding: 8px 16px;">🍔 Burgers</span>
          <span style="font-size: 11px; letter-spacing: 0.15em; text-transform: uppercase; color: var(--gold); border: 1px solid rgba(184,150,78,0.35); padding: 8px 16px;">🍟 Frites</span>
          <span style="font-size: 11px; letter-spacing: 0.15em; text-transform: uppercase; color: var(--gold); border: 1px solid rgba(184,150,78,0.35); padding: 8px 16px;">🔥 Plats chauds</span>
          <span style="font-size: 11px; letter-spacing: 0.15em; text-transform: uppercase; color: var(--gold); border: 1px solid rgba(184,150,78,0.35); padding: 8px 16px;">🇺🇸 Style américain</span>
        </div>
      </div>
    </div>

    <!-- Content -->
    <div class="reveal reveal-delay-2">
      <span class="section-label">Option originale</span>
      <h2 class="section-title">Notre Food Truck<br><em style="font-style:italic; color:var(--gold-light);">à votre service</em></h2>
      <p class="section-body" style="margin-bottom: 32px;">Envie d'une ambiance conviviale et décalée pour votre événement ? MJ Traiteur dispose d'un <strong style="color: var(--white); font-weight: 500;">food truck style américain</strong>, entièrement équipé pour cuisiner sur place.</p>
      <p class="section-body" style="margin-bottom: 40px;">Burgers généreux, frites dorées, plats chauds préparés à la minute — une expérience gourmande et festive, directement intégrée à votre événement, disponible <strong style="color: var(--white); font-weight: 500;">sur demande</strong>.</p>

      <div style="border-left: 2px solid var(--gold); padding-left: 24px; margin-bottom: 40px;">
        <p style="font-family: 'Cormorant Garamond', serif; font-size: 20px; font-style: italic; color: var(--gold-light); line-height: 1.5;">Une touche américaine qui fait l'unanimité, pour les grands comme pour les petits.</p>
      </div>

      <a href="tel:0666218892" style="display: inline-block; font-size: 11px; letter-spacing: 0.25em; text-transform: uppercase; color: var(--gold); border: 1px solid rgba(184,150,78,0.4); padding: 14px 32px; text-decoration: none; transition: all 0.3s;" onmouseover="this.style.background='var(--gold)'; this.style.color='var(--black)';" onmouseout="this.style.background='transparent'; this.style.color='var(--gold)';">Renseignez-vous sur le food truck</a>
    </div>

  </div>

  <div style="position:absolute; bottom:0; left:0; right:0; height:1px; background: linear-gradient(to right, transparent, rgba(184,150,78,0.2), transparent);"></div>
</section>

<!-- CONTACT -->
<section id="contact">
  <span class="section-label reveal" style="display:block; text-align:center;">Commençons ensemble</span>
  <h2 class="contact-title reveal">Contactez-nous</h2>
  <p class="contact-sub reveal">Un projet ? Une question ? Nous répondons rapidement.</p>

  <div class="contact-grid">
    <a href="tel:0666218892" class="contact-card reveal reveal-delay-1">
      <span class="contact-icon">📞</span>
      <span class="contact-label">Téléphone</span>
      <span class="contact-value">06 66 21 88 92</span>
    </a>
    <a href="mailto:mjtraiteur13@gmail.com" class="contact-card reveal reveal-delay-2">
      <span class="contact-icon">✉️</span>
      <span class="contact-label">Email</span>
      <span class="contact-value">mjtraiteur13@gmail.com</span>
    </a>
    <a href="https://instagram.com/mjtraiteur13" target="_blank" class="contact-card reveal reveal-delay-3">
      <span class="contact-icon">📸</span>
      <span class="contact-label">Instagram</span>
      <span class="contact-value">@mjtraiteur13</span>
    </a>
    <a href="https://share.google/fCRqLrAyAk4pEcodw" target="_blank" class="contact-card reveal reveal-delay-4">
      <span class="contact-icon">⭐</span>
      <span class="contact-label">Google</span>
      <span class="contact-value">+80 avis · Note 5★</span>
    </a>
  </div>

  <div class="reveal">
    <a href="tel:0666218892" class="cta-main"><span>Demander un devis gratuit</span></a>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-logo"><span>MJ</span> Traiteur Événementiel</div>
  <p class="footer-copy">© 2026 MJ Traiteur — PACA & Bouches-du-Rhône</p>
  <div class="footer-social">
    <a href="https://instagram.com/mjtraiteur13" target="_blank">@mjtraiteur13</a>
  </div>
</footer>

<script>
  // Navbar scroll
  const nav = document.getElementById('nav');
  window.addEventListener('scroll', () => {
    nav.classList.toggle('scrolled', window.scrollY > 60);
  });

  // Scroll reveal
  const reveals = document.querySelectorAll('.reveal');
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        e.target.classList.add('visible');
      }
    });
  }, { threshold: 0.12 });

  reveals.forEach(r => observer.observe(r));
</script>
</body>
</html>
